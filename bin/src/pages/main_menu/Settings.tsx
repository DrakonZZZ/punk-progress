import { Link } from 'react-router-dom';

const Settings = () => {
  return (
    <main className="setting-main-container">
      <Link to="/">back</Link>
      <div>Settings</div>
    </main>
  );
};

export default Settings;
